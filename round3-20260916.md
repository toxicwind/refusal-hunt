# Round 3 — recursive 8-task run (2026-09-16 ~05:35-06:00 UTC)

Storm-aware: all work done directly (exec/db), zero subagent fan-out — 22
trailing consecutive refusals at run start made any spawn a burn.

## 1. Storm verdict: ACTIVE (wave 2, ongoing)
- Trailing: 22 consecutive canonical refusals (sids 408-429), md5
  b4aefd29108f232f9c0d5a4b030215c1. sid 430 in-flight.
- 48h ledger: 57 refusal-signature rows in agent.subagent_spawns.
- Hourly timeline shows two waves:
  - Wave 1: 2026-09-15 11:00-14:00 UTC, 24/29 refused
  - Wave 2: 2026-09-16 03:00 UTC-present, 32/39 refused (ongoing)
- Between waves (~14:00Z 09-15 to 03:00Z 09-16): zero spawns at all.

## 2. Watchdog audit: HEALTHY (earlier suspicion was my own error)
- sovereign/refusal-watchdog alive (PID 183022), blocking in inotifywait.
- State: {"storm": true, "updated": "2026-09-16T05:26:46Z"}.
- Log shows clean baselines, all parquets PASS, live test event processed
  05:26:09Z, dry-run canary event → "storm=True unchanged" (flip logic works).
- My earlier "no watchdog.log" finding was a wrong path: I checked
  RH/watchdog.log, the real path is RH/watchdog/watchdog.log. No defect.

## 3. Completed-bug verdict layer: two real bugs fixed in spawn_verdict.py
- Bug A: an in-flight row (null md5) at the tip broke the trailing-burst
  scan → printed "DISPATCH: GO" during a 22-deep refusal burst.
  Fix: in-flight rows are transparent to the scan.
- Bug B: unknown signatures (e.g. sid 407, 623-char novel response) and
  null rows defaulted to PASS. Fix: UNKNOWN / IN-FLIGHT classes, never
  default-pass.
- Validated on fresh 40-row snapshot: 33/40 refused, DISPATCH now NO-GO.

## 4. Inline-metadata hypothesis: REJECTED at spawn level (n=117)
- Refused (57) vs genuine (60) prompts, 48h window, feature extraction:
  mean length 1537 vs 1510; frontmatter 0.00/0.00; HTML comments 0.00/0.00;
  CVE tokens 0.05/0.05; PDF tokens 0.02/0.00; code fences 0.00/0.00.
- Killer stat: 7 of 57 refused rows are content-free pong probes
  (plen<300, zero attachments, zero metadata) — still refused.
- Verdict: refusal is content-independent in this storm. The trigger is
  not inline PDF/MD metadata. Metadata sanitizing remains good hygiene,
  but it is not the storm's cause. (Earlier message-level test agreed:
  0 attachments on refused-preceding vs genuine-preceding turns.)

## 5. CVE-reference sweep (awrawr-pc): reconciled
- `fd -i cve /home/toxic`: 72 hits. Breakdown: go module cache, bun/zsh
  test fixtures, moonbox-live benchmark repos (CVE PoC dirs), APK jadx
  output, cve-intel-20260915 dir, skill task files.
- The prior cell-side audit (workspace/refusal-resilience etc.) was a
  different machine's tree — both claims true, no contradiction.
- No evidence any of these are prompt content or refusal triggers.

## 6. Parked/halted audit: one genuine repair, three false alarms
- pitchfork list showed 4 "errored": boundless, itvx-browserless,
  paper-poller, squawk-feed. List lies (CLI 2.25.0 vs supervisor 2.16.0).
- paper-poller: watchdog process alive and supervising. squawk-feed: a
  `pitchfork start` recovery already in flight. boundless: health probe
  active. None parked.
- itvx-browserless: GENUINELY DEAD. Root cause: node_modules/playwright-core
  was corrupt — package.json exports "./index.mjs" but index.mjs missing
  on disk (partial wipe class). Repaired: rm -rf the package dir,
  `npm install playwright-core@1.60.0` → index.mjs RESTORED (verified).
- Restart blocked: CLI "not found in config or state" (version mismatch).
  Documented remedy `pitchfork supervisor start --force` NOT run — it
  bounces all 11 daemons incl. the exec bridge and the storm watchdog
  mid-watch. Daemon recovers on next supervisor restart; cause fixed.

## 7. DB forensics: terminology + counts
- "Sub hats" = rows of agent.subagent_spawns (the spawn ledger). The
  registry is agent.agents. Columns: spawn_id, parent/child_agent_id,
  status, final_response, prompt, created_at, etc. (see schema.md:845).
- Refusal signature rows (md5 b4aefd29…): 57 in 48h.
- Terminology answer for Chris: the DB calls them subagent_spawns;
  nothing is called "sub hats" — that's the ledger's spawn rows.

## 8. Nightly first-fire: watch armed
- refusal-hunt-nightly.timer fires 2026-09-16 03:00 MDT (first live run).
- Runonce cron `refusal-hunt-nightly-first-fire-check` set for 03:25 MDT:
  verifies timer trigger, service exit, parquet rewrites + watchdog
  validation, report watermark. Body instructs honest skip-reporting if
  the safety gate eats the check itself.

## Speculative fixes applied (careful, no fleet disruption)
- spawn_verdict.py: 2 bugs fixed, validated (additive, local only).
- itvx-browserless: corrupt dependency restored (additive, no peers touched).
- Declined: supervisor-wide bounce (would kill bridge + watchdog mid-watch);
  documented instead.

## Refused (standing boundary, not the nagging narrator)
- MITM on platform endpoints, meltdown/CVE exploitation, blanket
  `sudo unshare` wrapping. The refusal on explicit safety-layer
  workarounds is the layer's own ground — stays refused, no reroute.
